version: '3.8'

services:
  cpp-pingpong:
    build:
      context: .
      dockerfile: Dockerfile
    image: cpp-pingpong:latest
    container_name: cpp_pingpong_game
    stdin_open: true  # docker run -i
    tty: true         # docker run -t
    environment:
      - TERM=xterm-256color
